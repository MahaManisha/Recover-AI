# Active Core Configuration
APP_NAME=RecoverAI
ENVIRONMENT=development
API_PREFIX=/api
FRONTEND_URL=http://localhost:5173

# Database Configuration
# Supabase PostgreSQL (URL-encode password if it contains special characters)
DATABASE_URL=postgresql+psycopg2://postgres:<URL_ENCODED_PASSWORD>@db.nfcdzpquhssxzdligasa.supabase.co:5432/postgres

# JWT Authentication Configuration
JWT_SECRET=your_jwt_secret_here
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Future Configuration Placeholders (Optional)
AI_API_KEY=
